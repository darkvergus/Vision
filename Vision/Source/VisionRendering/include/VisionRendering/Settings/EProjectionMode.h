#pragma once



namespace VisionRendering::Settings
{
    /**
    * Projection modes, mostly used for cameras
    */
    enum class EProjectionMode
    {
        ORTHOGRAPHIC,
        PERSPECTIVE
    };
}